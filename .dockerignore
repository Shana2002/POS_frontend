node_modules
dist
.git/
.gitignore

# Generated by test runs.
test-results
playwright-report
*.tsbuildinfo

# Playwright specs are not part of `tsc -b` (tsconfig.node.json includes only
# vite.config.ts, eslint.config.js and playwright.config.ts), and the e2e suite
# stubs the network, so it has no business in a serving image.
e2e

.env
.env.local

*.md
Dockerfile
.dockerignore
